# Paper data identities

No benchmark text or model weights are distributed.
See [LICENSES.md](LICENSES.md) for pinned sources and redistribution terms.
Each artifact bundle contains `record-plan.json`, `protocol.json` and
`compatibility.json`, preserving per-record IDs/grouping, dataset hashes and
model identity. Both sizes used the same selected input records.
